# ebook-server

电子书阅读器后端服务，为配套的 Android 客户端提供账号、认证、评论与操作日志能力。开源自托管项目，部署者可能不持有域名、公网 IP 或 HTTPS 证书。

## 账号与身份

**登录主标识**:
账号的唯一凭据，用于登录与找回密码，当前由邮箱承担。**不可变**——注册即确定，没有变更流程。
_Avoid_: 用户名、账号名、主键

**展示用名**:
账号对外显示的名字，注册时自动生成、用户可自改。非唯一，不承担任何凭据职责。可变性只分配给这个字段。
_Avoid_: 用户名（易与登录主标识混淆）、昵称

**账号根标识**:
账号创建时分配、永不改变的编号，是服务端内部引用账号的唯一方式，也是凭证签发的依据。
_Avoid_: ID、主键、用户 ID

**二次确认**:
敏感操作前要求用户再次证明对当前凭据的占有。注销账号用邮箱验证码确认，改密码用旧密码确认——两者强度对等，使仅持有 access token 的攻击者无法接管账号。
_Avoid_: 二次验证、双因子（本系统无第二因子）

**验证码**:
分发的 6 位短码，作为「此邮箱归我」的一次性凭据（注册、找回密码）或敏感操作的「二次确认」（注销）。验证码按所在流程相互隔离——注册发码不会占用找回密码的可用性，且一个流程下发的码不能被另一流程使用。
_Avoid_: OTP、动态口令、激活码

## 账号生命周期

**账号注销**:
账号的终结操作，需邮箱验证码确认。注销后无法撤销、无法再用该邮箱对应的原账号登录。
_Avoid_: 删除账号、销号、关闭账号

**匿名化**:
注销时对账号执行的动作——抹去所有可识别信息（登录主标识、展示用名、密码、头像）并让登录主标识占位化，但保留行本身以维持公开内容的完整性。账号因此不可登录、不可按邮箱检索，但公开内容仍能关联到它。
_Avoid_: 软删除（本系统明确不做，它会挡住邮箱重新注册并切断评论关联）、硬删除、清空、脱敏

**数据导出**:
用户取走自己数据的操作，产出一份不含凭据的结构化副本。只在用户主动请求时发生，是本系统提供的数据可携方式。
_Avoid_: 备份、数据下载、导出备份

**公开内容**:
不属于任何单个用户私有、但由用户产生的内容，目前指评论。账号注销后公开内容保留，作者显示为匿名化后的名字。
_Avoid_: 用户数据（公开内容不属于导出范围的争议地带，见 ADR）

**章节评论**:
归属到某个书源章节的评论。以「书源章节 URL」为聚合键（精确匹配），章节名/书名是展示快照。
后端不消费书源数据，URL 原样存储、不做格式或存在性校验。
_Avoid_: 章评、楼中楼（无回复层级）、评论主题

**书籍级评论**:
不归属任何章节的评论（章节字段为空串），面向整本书或通用场景。与章节评论同一张表，
仅以「章节字段是否为空」区分。
_Avoid_: 通用评论、无主题评论（语义含糊）

## 操作日志

**操作日志**:
系统自动记录的请求元信息，用于审计与排障。用户可读、不可写、不可删——包括注销账号时也不删除。
_Avoid_: 审计日志、访问日志、行为记录
